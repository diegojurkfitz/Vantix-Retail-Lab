# Terraform
.terraform/
# NOTA: .terraform.lock.hcl DEVE ser versionado (reprodutibilidade).
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log

# Variáveis (permite apenas o exemplo)
*.tfvars
!terraform.tfvars.example

# Build artifacts da Lambda
terraform/build/
*.zip

# Plano salvo (artefato transitório)
*.tfplan

# Credenciais / segredos
.env
.env.*
*.pem
*.key
credentials
secrets/

# Sistema
.DS_Store
Thumbs.db
